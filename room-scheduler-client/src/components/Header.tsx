import {FC} from "react";
// import { Context } from "../index";
// import { observer } from 'mobx-react-lite';

export const Header: FC = () => {
    return (
        <header>
            <h1>Яндекс Переговорки</h1>
        </header>
    )
}

// export default observer(Header);